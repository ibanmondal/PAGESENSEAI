# PageSense AI — backend dependencies
# Core API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0

# Retrieval & embeddings
sentence-transformers>=2.7
numpy>=1.26

# Agent orchestration
langgraph>=0.2
langchain-core>=0.2

# LLM providers (install only the ones you have keys for)
openai>=1.30
google-generativeai>=0.7

# Dev / eval
pytest>=8.0
httpx>=0.27
